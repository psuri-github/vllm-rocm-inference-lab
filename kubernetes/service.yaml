apiVersion: v1
kind: Service
metadata:
  name: vllm-server
  namespace: vllm
spec:
  type: ClusterIP
  selector:
    app: vllm-server
  ports:
    - name: http
      port: 8000
      targetPort: http
